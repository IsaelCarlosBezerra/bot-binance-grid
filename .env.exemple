# Banco de dados
DATABASE_URL=postgresql://usuario:senha@localhost:5432/bot-binance-grid?schema=public

# Segredo para geração dos tokens JWT (mínimo 32 chars)
JWT_SECRET=gere-um-valor-aleatorio-longo-aqui

# Segredo para criptografar as API Keys da Binance no banco (mínimo 32 chars)
ENCRYPTION_SECRET=gere-outro-valor-aleatorio-longo-aqui
